# AI Builder v2.0 — Netlify デプロイ（Next.js）
# Vercel を推奨。Netlify 利用時は @netlify/plugin-nextjs が必要です。

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
